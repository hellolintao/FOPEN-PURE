# FOPEN-PURE · Design Context

> 给所有视觉/前端 agent 用：动手前必读。无此 context 的页面会回到"AI 通货"。

## Users

**面向**：网球俱乐部内部成员（球员）与俱乐部管理员，规模十到数百人。
**使用场景**：
- 球员：球场边/休息间隙、报名前、对局结束、想看自己积分/排名时
- 管理员：开赛前夜整理名单、比赛日现场抽签、轮次结束确认比分

设备：均在微信小程序内（中端安卓 + iPhone 为主），亮度场景从俱乐部室内灯光到户外正午阳光跨度极大 → **浅色优先**。

## Brand Personality

**三词定调**：能量（Energy）· 球场感（Stadium）· 不讨好（Unapologetic）。

**它应该感觉像**：
- 一本网球年鉴的彩页 — 大号年份、超大球员特写、印刷网点；
- 球场计分牌特写 — 数字粗到撑爆视野；
- 运动品牌零售店海报 — 青柠绿撞墨黑，倾斜文字条像贴纸；
- 一份赛事册子内页 — 排名表本身就是版面而不是表格。

**它绝不应该是**：
- SaaS 后台风（圆角卡片网格 + 灰文 + 中性色）；
- 苹果医疗 app 风（米白色 + 极淡 stroke）；
- 任何"赛博朋克 / 霓虹 / 渐变深色"风；
- 任何"暖色调亲切社区"风。

## Aesthetic Direction

**总调**：Light-theme Sport Maximalism。
**基础调色**（已存在，保留并升级）：青柠绿 `#BEE645` 作为唯一品牌强调色；墨黑 `#0A0A0A` 与近白 `#FAFAFA` 做主底。中性色全部 tint 朝青柠绿那一侧的暖黄方向偏 0.005–0.01 chroma（OKLCH 实现）。

**色彩比例（60/30/10）**：
- 60% 近白/极浅暖灰底（页面 / 容器）
- 30% 墨黑（标题、关键数据、边）
- 10% 青柠绿（CTA、live 状态、用户最关心的那一个数字、tape 装饰条）
- 危险/告警/成功色为辅，仅在状态语义需要时出现，不当装饰

**字体策略**（避开 reflex font list）：
- **数字与西文 display**：Anton（远程加载，运动场计分牌气质，几何 condensed black）— 用于比分、名次、积分、年份、巡回赛代号。
- **中文 display**：PingFang SC Heavy/Black（系统）+ 极强的 letter-spacing 控制，配合超大字号制造冲击。
- **正文**：PingFang SC Regular（系统）。
- **绝不使用**：Inter / DM Sans / Space Mono / Fraunces / Plus Jakarta / IBM Plex 全家族。

**版式语言**：
- 大量"印刷感横排标签条"（tape）：黑底白字斜切或全宽，承载 "ROUND 03 · 半决赛" 这类元信息；
- 数据用超大数字 + 极小语义标签的对比；
- 排版主动不对称：左对齐 + 大留白 + 偶尔超出页边的元素；
- 圆角分两档：内容卡片 `--radius-md`（克制），强调容器/Hero `--radius-lg`；按钮强语义场景偶用 `--radius-pill`，但不要把所有 chip 都做 pill；
- 阴影克制，单层柔阴影；强调用边框 + 描边 + 偏移色块，而非阴影深浅；
- 所有"列表"必须有视觉节奏 — 不允许出现等高同色的纯重复列表。

## Differentiation

**这个小程序最该让人记住的**：查比赛成绩和排名的体验独一无二。

落地承诺：
- `rank` 页是整个项目的视觉锚 — 必须像翻开一本年鉴；
- `tournament-brackets` 必须像赛事册的对阵页；
- `round-settlement` 是仪式感时刻，必须有揭晓动效；
- `tournament-score` 现场计分必须像计分牌特写。

## Design Principles

1. **数据是英雄**（Data is the Hero）：每个屏上必须有一个被刻意做大、做粗、做高对比的数字或标签。其他都为它让路。
2. **节奏优于均匀**（Rhythm over Uniformity）：宁可一个区段紧密一个区段开阔，也不要全屏匀速。
3. **品牌锚色稀缺即强**（Lime is Scarce）：青柠绿一屏最多两处，超过就稀释了它。其他全部用墨黑/近白/中性。
4. **形式呼应运动场**（Echo the Stadium）：tape 横条、计分牌字、阶段路径、对峙构图 — 用版面语言而非图标语言传达运动感。
5. **不依赖装饰填空**（No Filler Decor）：禁用 border-left 条、gradient 文字、glass、磨砂、sparkline 装饰、模板化卡片网格、icon-with-rounded-square-above-heading。
6. **小程序原生克制**（Respect the Platform）：不滥用 `wx.showToast`/`showLoading`；遵循已商定的 v2.1 loading/empty/error 矩阵；自定义动效不阻塞输入。

## Constraints

- 平台：微信小程序原生（WXML/WXSS/JS），无 CSS-in-JS、无 Vue/React
- 单位：颜色/字体可以 px，布局/字号优先 `rpx`
- 字体加载：通过 `wx.loadFontFace` 加载远程字体（仅西文 display）；中文不远程加载
- token：颜色优先 OKLCH（在小程序里 wxss 可用 css4 颜色函数）；通过 `tokens.wxss` 全局定义
- 兼容：保持现有 token 名（`--color-lime` 等），只允许扩展不允许重命名以免破坏既有页面
