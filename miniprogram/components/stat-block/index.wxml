<view class="stat-block anim-soft-pop">
  <view class="text-stat">
    <number-counter value="{{value}}" suffix="{{suffix}}"></number-counter>
  </view>
  <text class="text-label">{{label}}</text>
</view>
